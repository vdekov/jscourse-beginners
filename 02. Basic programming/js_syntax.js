// Single-line comment
/* Multi-line
comment */
var operand = 2 + 5; // operand operator expression semicolon
window.document;     // property reference
window['document'];  // property reference

if ( true ) {        // block
   alert( 'true' );
}

function foo() {     // function declaration
   return null;
}

var myvar = null;    // define variable & assign `null`
var bar;             // declare a variable (value is `undefined`)
window.foo;          // accessing unexisting property